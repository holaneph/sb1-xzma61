import { Component } from '@angular/core';

@Component({
  selector: 'ns-user-profile',
  template: `
    <StackLayout>
      <Label text="User Profile" class="h2 text-center"></Label>
      <!-- Add user profile content here -->
    </StackLayout>
  `,
})
export class UserProfileComponent {}